## EP-001-UC-001-S003 — Generare il sito statico da un Article (ARTICLE-PAGE, LISTING-PAGE, HOME-PAGE)

**Decision:** Con quale meccanismo il sistema trasforma un `Article` valido in output HTML statico distribuibile — ARTICLE-PAGE del nuovo articolo, più aggiornamento di LISTING-PAGE e HOME-PAGE — sostituendo la generazione server-side (SSR) attuale.

As Autore/Editore, voglio che il mio articolo compaia come pagina statica generata dal sito so that i visitatori possano leggerlo senza dipendere da un processo server sempre attivo.

| Field | Value |
|---|---|
| **Epic** | EP-001 — Rilancio del blog personale come presenza professionale |
| **UC** | UC-001 — Autore pubblica un nuovo articolo |
| **Pattern** | Major effort isolation |
| **AT rows** | AT-UC-001 righe: 1-5 (happy path, limitatamente a "build riuscita → ARTICLE-PAGE/LISTING-PAGE/HOME-PAGE generate correttamente"; il deploy stesso è fuori da questa story, vedi [[EP-001-UC-001-S005]]), righe happy path "abstract assente" e "immagine di sintesi assente" |

### Acceptance criteria
- Given un `Article` valido prodotto da [[EP-001-UC-001-S001]], when il build statico viene eseguito, then genera una ARTICLE-PAGE raggiungibile allo slug dell'articolo con metadati e contenuto corretti
- Given un `Article` valido pubblicato, when il build statico viene eseguito, then la LISTING-PAGE include una voce per il nuovo articolo
- Given un `Article` valido pubblicato, when il build statico viene eseguito, then la HOME-PAGE (caso particolare cronologico della LISTING-PAGE) riflette il nuovo articolo secondo l'ordine cronologico
- Given un `Article` con abstract assente, when il build statico viene eseguito, then l'abstract mostrato in ARTICLE-PAGE/LISTING-PAGE/HOME-PAGE è calcolato automaticamente dal corpo dell'articolo
- Given un `Article` con immagine di sintesi assente, when il build statico viene eseguito, then viene usata un'immagine di fallback predefinita in ARTICLE-PAGE/LISTING-PAGE/HOME-PAGE

### Design pipeline
Before any implementation, complete in order:
- [x] `/software-design`        — coupling, ownership, accidental complexity
- [x] `/hexagonal-architecture` — ports, adapters, composition root
- [x] `/parse-dont-validate`    — domain types and invariants
- [x] `/sw-practices`           — naming, error handling, bootstrap

Hexagonal design artefact: [docs/architecture/hexagonal.md](../architecture/hexagonal.md)
Parse-dont-validate artefact: [docs/design/ssg-page-generation.md](../design/ssg-page-generation.md)

### Next steps after agreement
- [x] `/acceptance-tests EP-001-UC-001-S003` — story-level AT table (narrower than UC level): [AT-EP-001-UC-001-S003](../acceptance-tests/AT-EP-001-UC-001-S003-generare-sito-statico-ssg.md)
- [ ] `/story-size EP-001-UC-001-S003`       — assign XS / S / M / L / XL / XXL

### Open questions
- Meccanismo di paginazione della LISTING-PAGE non deciso (già segnalato come Open Issue in EP-001 e in UC-003) — non blocca questa story se si assume una LISTING-PAGE senza paginazione come primo incremento, ma va riallineato quando UC-003 verrà splittata.
- **Decision needed (non architetturale):** quale asset usare come immagine di fallback predefinita (AC "immagine di sintesi assente"). Non è una scelta di design ma di contenuto/brand — va confermata da Luca prima dell'implementazione. `assets/images/ostia_sea_top_image.webp` esiste già nel repo ma è oggi lo sfondo fisso del layout (`layout.rs:20`), non pensato come immagine segnaposto per articoli — riusarlo richiede conferma esplicita, non è un default silenzioso.

### Escalation
Questa decisione corrisponde a [ADR-004](../adr/ADR-004-ssg-islands-replaces-ssr.md) — **"SSG + islands al posto di SSR"**, formalizzata il 2026-08-24. Resta la decisione più impattante e meno reversibile dell'intera UC-001 (tocca l'intero bootstrap dell'applicazione, non solo la pubblicazione articoli); il meccanismo esatto di implementazione è demandato al design pipeline di questa story (ADR-004, sezione Technical Notes).

### Dependencies
- Richiede il tipo `Article` e la porta `ContentSource` di [[EP-001-UC-001-S001]].
- Richiede `ContentSource::list_published` — oggi `NotImplemented` su entrambi gli adapter, esplicitamente deferito a questa story da [[EP-001-UC-001-S002]] (vedi il suo Decisions Log, 2026-08-24: "di competenza di S003, LISTING-PAGE, non di questa story").
- Estende `Article` (S001) con un campo `body` (contenuto markdown) — assente oggi: `FilesystemContentSource` legge solo il blocco YAML e scarta tutto ciò che segue la seconda fence `---`.

### Decisions Log
| Date | Decision | Reasoning | Alternatives Considered |
|---|---|---|---|
| 2026-08-24 | `Article` guadagna un campo `body: String`, validato non-vuoto nello smart constructor (`Article::new`), stesso pattern di `date`/`slug`/`tags`/`title`. `FilesystemContentSource` legge il body come tutto il testo dopo la seconda fence YAML | Serve per generare il contenuto di ARTICLE-PAGE (AC-1) e come sorgente per l'abstract calcolato (AC-4); un body vuoto non è un articolo pubblicabile, quindi è un invariante reale da rifiutare a costruzione, non un default silenzioso | Lasciare `body` come `Option<String>` senza validazione — scartata: sposterebbe il controllo "articolo senza contenuto" a valle, in un punto meno vicino alla causa |
| 2026-08-24 | Il calcolo dell'abstract effettivo (truncation del `body` quando `abstract_text` è assente) e dell'immagine di sintesi effettiva (fallback a un asset predefinito quando `image` è assente) vive nella **presentation layer** (view-model consumato da ARTICLE-PAGE/LISTING-PAGE/HOME-PAGE), non in `Article` | `Article` resta un valore puramente parsato (parse-dont-validate senza logica derivata): la lunghezza di truncation e il path dell'asset di fallback sono decisioni di presentazione/brand, non invarianti del dominio. Tenerle fuori dal dominio evita che `Article` conosca path di asset infrastrutturali | Metodi calcolati su `Article` stesso (`effective_abstract()`, `effective_image()`) — scartata: accoppierebbe il dominio a una policy di rendering che può cambiare senza che il significato di "articolo" cambi |
| 2026-08-24 | `ContentSource::list_published` viene implementato ora su entrambi gli adapter (filesystem: enumera i file `.md` in `articles_dir` e riusa `get_by_slug` per ciascuno; in-memory: restituisce gli articoli inseriti). L'ordine cronologico richiesto da HOME-PAGE (AC-3) è una policy applicata a valle, in fase di build della pagina, non una garanzia del contratto della porta | La porta risponde solo "quali articoli esistono", senza impegnarsi su un ordine — tenere l'ordinamento fuori dal port contract evita di dover ridefinire la porta se in futuro cambia il criterio (es. per-tag, non solo cronologico) | Garantire l'ordine già dentro `list_published` — scartata: accoppierebbe il contratto della porta a una policy di presentazione che può cambiare indipendentemente |
| 2026-08-24 | Composition root SSG: `leptos_actix::generate_route_list_with_ssg` produce le route + uno `StaticRouteGenerator`; ARTICLE-PAGE è una `StaticRoute` con `prerender_params` alimentato da `ContentSource::list_published()` (uno slug per articolo pubblicato); LISTING-PAGE/HOME-PAGE sono `StaticRoute` senza parametri. `StaticRouteGenerator::generate(&leptos_options)` scrive l'HTML in `site-root` **una volta, a build time**; in produzione il processo termina dopo la generazione (nessun listener always-on, coerente con ADR-004) — verificato leggendo il sorgente di `leptos_actix` 0.8.7 (`write_static_route` scrive file `.html` reali) e l'esempio ufficiale `examples/static_routing` di leptos-rs/leptos. In dev (`cargo leptos watch`) lo stesso binario può continuare a tenere un listener attivo per comodità di sviluppo — non contraddice ADR-004, che vincola solo la produzione | Verificato da fonte primaria (sorgente crate + esempio ufficiale) prima di fissare la decisione, non assunto; evita di scoprire a metà implementazione che il meccanismo richiede un server sempre attivo anche in produzione | Scrivere un renderer HTML custom senza passare da `leptos_router`/`leptos_actix` (rendering manuale di ogni `Article` a stringa) — scartata: reinventerebbe la gestione di route/param/hydration già offerta dal framework, aumentando complessità accidentale |
| 2026-08-24 | Rivista in `/hexagonal-architecture`: nessun primary port dedicato (`BuildStaticSite` o simile) — la composition root (`src/main.rs`, riscritto) è l'unico chiamante e l'unica implementazione, quindi funge da primary adapter *e* primary port insieme (una chiamata di funzione diretta). ArticlePage/ListingPage/HomePage vivono in un nuovo modulo `src/pages/` (non `presentation/` dello sketch originario dell'assessment — riconciliato con la convenzione flat già in uso da `domain/`/`adapters/`), come `#[component]` semplici (non `#[island]`): niente idratazione, niente interattività. Il view-model (`effective_abstract`/`effective_image`) vive in `src/pages/view_model.rs`, non in `domain/` (coerente con la riga precedente) | Un primary port qui aggiungerebbe indirection senza un secondo chiamante da servire — va introdotto quando (se) servirà davvero. Le pagine non sono island perché non hanno bisogno di interattività client-side: tutto il contenuto è risolto e scritto in HTML a build time | Un trait `SiteGenerator` primary port attorno alla composition root — scartata, nessun secondo chiamante oggi |
| 2026-08-24 | Chiarito il data flow per articoli in ARTICLE-PAGE/LISTING-PAGE/HOME-PAGE: nessuna `#[server]` function né `Resource` Leptos è necessaria. La composition root risolve ogni `Article` in modo sincrono (via `ContentSource`) *prima* del rendering e lo passa ai page component come prop ordinaria (o via `provide_context` per il callback di route). "Server" e "renderer" sono lo stesso processo in-process durante `generate()` — non c'è uno split client-server reale da mediare per questi dati | Evita di introdurre un meccanismo (server function/Resource) pensato per un client-server reale dove non serve; i page component restano testabili come qualunque componente Leptos server-rendered, senza contesto asincrono | Usare un `#[server]` fn comunque, per uniformità con un futuro ipotetico client-fetch — scartata: complessità accidentale per un bisogno che non esiste in questa story |
| 2026-08-24 | Rivista in `/parse-dont-validate`: `Article` guadagna `body: Body` (non `String` grezzo), con `Body::parse(raw: Option<&str>) -> Result<Body, BodyError>`, `BodyError::Missing` unico variant (nessun "malformed" per markdown libero). Inoltre `abstract_text`/`image` diventano `Option<Abstract>`/`Option<ImagePath>` (non più `Option<String>` grezzo): entrambi normalizzano un valore presente-ma-vuoto (`abstract: ""` in frontmatter) a `None`, senza errore — "assente" è uno stato legittimo, non un fallimento | Gap reale scoperto analizzando l'AC "abstract/immagine assente": oggi `Option<String>` grezzo lascia passare `Some("")`, che il fallback di S003 (`effective_abstract`/`effective_image`) leggerebbe come "presente" pur non essendo un valore usabile — un illegal state raggiungibile da frontmatter reale, non ipotetico | Lasciare `abstract_text`/`image` come `Option<String>` e normalizzare dentro `effective_abstract`/`effective_image` — scartata: sposterebbe la stessa normalizzazione a valle, in più punti se in futuro altri consumer leggessero `Article.abstract_text` direttamente |
| 2026-08-24 | L'output di `effective_abstract`/`effective_image` (view-model, `src/pages/view_model.rs`) resta `String`/`&str` semplice, nessun nuovo tipo — deroga esplicita a parse-dont-validate sull'output: il valore è consumato una sola volta, interpolato direttamente nella view, mai ripropagato né ri-validato altrove; la funzione di truncation garantisce già per costruzione un testo non vuoto | Un wrapper qui non eliminerebbe alcuno stato illegale in più di quanto la funzione già garantisca — sarebbe cerimonia senza protezione aggiuntiva | `EffectiveAbstract(String)` newtype — scartata, nessun invariante da proteggere che il chiamante unico non rispetti già |
| 2026-08-24 | `/sw-practices`: adottato `pulldown-cmark` (0.13.4, aggiornato 2026-05-20) per convertire `Body` (markdown grezzo) in HTML — verificato su crates.io prima di adottarlo, stesso standard di `docs/design/article.md` (S001). Il rendering vive in `src/pages/`, non nel dominio: `Body` resta testo grezzo, il dominio non deve sapere che l'HTML esiste. Dipendenza non ancora aggiunta a `Cargo.toml` — deferita al commit `feat` che la userà davvero in `article_page.rs`, per non lasciare una dipendenza aggiunta-ma-inutilizzata nel manifest nel frattempo | Nessuna alternativa seria in Rust puro per CommonMark con la stessa maturità/manutenzione; verificare la manutenzione prima di adottare è pratica già stabilita nel repo | `comrak` (GFM, più pesante) — non necessaria per un blog personale senza requisiti GFM espliciti |
| 2026-08-24 | `/sw-practices`: raffinato il data-flow deciso in `/hexagonal-architecture` verificando la vera API di `leptos_router` (`examples/static_routing/src/app.rs`) — una `<Route view=ArticlePage .../>` instrada verso una funzione a zero argomenti, non può ricevere `Article` come prop diretta da un parent. Meccanismo corretto: `ArticlePage` legge `:slug` via `use_params_map()`, poi risolve `Article` sincronamente tramite `expect_context::<ContentSource>()` + `get_by_slug`, con il `ContentSource` fornito una sola volta dalla composition root via `provide_context` attorno a `<Router>`. Resta vero che non serve `#[server]`/`Resource` | Scoperto verificando la sintassi reale invece di assumerla — evita di scoprire l'incompatibilità a metà implementazione | — |
| 2026-08-24 | `/sw-practices`: bootstrap effettivo in `src/` per la parte a basso rischio, verificata a compilazione (`cargo build`/`test` su target `ssr` e `hydrate`/wasm, 44 test verdi, nessun nuovo warning clippy oltre i 6 preesistenti sulla main): `Body`/`Abstract`/`ImagePath` (nuovi value object), `Article`/`RawFrontmatter`/`ArticleError` estesi, `ContentSource::list_published` implementato su entrambi gli adapter (filesystem: enumera `.md` in `articles_dir`, propaga il primo errore `Malformed` incontrato — nessuna decisione precedente lo specificava, scelta per coerenza con "mai inghiottire errori silenziosamente"; in-memory: restituisce gli articoli memorizzati), `FilesystemContentSource` cattura ora il body dopo la seconda fence YAML. **Non incluso in questo step** (deferito a un commit `feat` successivo, dopo `/acceptance-tests`+`/story-size`, stesso ritmo di S001): `src/pages/` (view-model, page component), riscrittura di `src/main.rs`/`src/app.rs` come composition root SSG — richiedono sintassi Leptos non ancora verificabile a compilazione senza il resto della story in atto | Stesso confine di rischio già usato in S001 (sw-practices bootstrap i value object a basso rischio, il port/adapter reale arriva in un feat successivo): value object e adapter mechanics sono pattern meccanici già consolidati nel repo, la composition root SSG richiede iterazione con il compilatore che non ha senso anticipare qui | Scrivere anche `main.rs`/`app.rs`/`pages/` a mano ora, senza poterli compilare in isolamento — scartata, rischio di codice sbagliato non verificato |
| 2026-08-24 | Guardrail: nessuna island può invocare una `#[server]` function a runtime — in produzione non esiste alcun server a rispondere dopo la generazione statica. I dati devono essere risolti interamente in fase di `generate()` e serializzati nell'HTML prerenderizzato. Le island esistenti (`DynamicHeader`, `LightDarkSwitch`) già rispettano questo vincolo (nessuna server function); vincolo esplicito per `/hexagonal-architecture` e per island future | Se non esplicitato ora, una futura island che chiama una server function funzionerebbe in dev (`cargo leptos watch`, server attivo) ma fallirebbe silenziosamente in produzione (nessun server) — rischio di regressione difficile da notare in un ambiente di sviluppo che nasconde il problema | — |
| 2026-08-30 | Riconfermato l'intero design pipeline con `/architecture-compass` + `/residuality` + `/software-design` + `/hexagonal-architecture` + `/parse-dont-validate`, questa volta con un vero checkpoint dopo ogni step (il primo passaggio, 2026-08-24, non ne aveva — vedi feedback_design_pipeline_needs_checkpoints). Estensione: fallback immagine (AC "immagine di sintesi assente") estesa a "referenziata ma assente su disco" — nuovo tipo `ResolvedImage { Own(ImagePath) \| Fallback { attempted: Option<ImagePath> } }`. `resolve_image` inizialmente proposta come funzione locale al composition root, non promossa a porta — **superata il giorno dopo, vedi riga 2026-08-31** | `/residuality`'s Stressor Analysis ha trovato un gap reale: un articolo può referenziare un'immagine mai committata nel repo contenuti o poi rimossa, caso distinto da "nessuna immagine impostata". `ResolvedImage` è un tipo dedicato (non una `&str` semplice) perché il risultato serve a due consumer che devono restare coerenti — il path da renderizzare e un warning di audit da loggare — non solo uno, a differenza della deroga già in vigore per `effective_abstract`/`effective_image` | Lasciare l'I/O dentro `effective_image` stesso — scartata, romperebbe la purezza già stabilita per il view-model |
| 2026-08-30 | Confermato da Luca: le immagini articolo vivono temporaneamente in `assets/images/` dentro il repo contenuti dedicato (nessuna nuova env var) — non in `assets/` di questo repo; l'asset di fallback è un nuovo SVG dedicato (`assets/images/article-image-not-found.svg`), non il riuso di `ostia_sea_top_image.webp` | Entrambe le decisioni erano bloccanti per completare la definizione di `resolve_image`/`ResolvedImage` sopra (senza sapere dove cercare il file e cosa mostrare al posto, il tipo non è specificabile); la collocazione delle immagini è dichiarata temporanea, da rivedere con una storia dedicata | Riusare `assets/` di questo repo per le immagini articolo — scartata da Luca, preferita la collocazione nel repo contenuti pur non ancora creato; riusare `ostia_sea_top_image.webp` come fallback — scartata da Luca, è lo sfondo fisso del layout |
| 2026-08-31 | Corretta la riga precedente: Luca ha giudicato debole "un solo chiamante, non promossa a porta" per `resolve_image`. `ContentSource` guadagna `image_exists(&self, image: &ImagePath) -> Result<bool, FetchError>` (stessa forma di `exists`, `FetchError` riusato); `resolve_image(source: &impl ContentSource, image: Option<&ImagePath>) -> Result<ResolvedImage, FetchError>` diventa domain service in `src/domain/image_resolution.rs`, parallelo a `ensure_slug_is_unique` — non più I/O grezzo nel composition root. `images_dir` diventa un campo interno di `FilesystemContentSource` (accanto ad `articles_dir`), non più config a livello di composition root | L'argomento "un solo chiamante" vale per un *primary port* (nessun secondo entry-point) ma non per questa I/O sul repo contenuti: ogni altro accesso allo stesso repo passa già per `ContentSource`, sostituibile in test con `InMemoryContentSource`; bypassarlo per le immagini avrebbe reso i test dei page component dipendenti da I/O reale su temp dir, rompendo la riga "view_model — None" della Testing strategy | Nuovo trait `AssetSource`/`ImageSource` dedicato ora — scartata, prematura: le immagini vivono oggi nello stesso repo contenuti degli articoli, nessuna ragione infrastrutturale per separarli già; resta backlog per quando (se) servirà davvero |

INVEST: I✓ N✓ V✓ E✓ S✓ T✓  |  1 Decision: ✓  |  Coherence: ✓
